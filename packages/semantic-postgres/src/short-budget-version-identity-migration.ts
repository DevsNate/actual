import shortBudgetVersionIdentitySql from '../migrations/0019_short_budget_version_identity.sql?raw';

export const shortBudgetVersionIdentityMigration = {
  filename: '0019_short_budget_version_identity.sql',
  sql: shortBudgetVersionIdentitySql,
} as const;
